name: Meu Pipeline Migrado
on: [push]

jobs:
  test-job:
    runs-on: ubuntu-latest
    container: python:latest
    steps:
      - name: Checkout do codigo
        uses: actions/checkout@v4
      - name: Rodando Test Job 1
        run: echo "This job tests something"

  test-job2:
    runs-on: ubuntu-latest
    container: python:latest
    steps:
      - name: Checkout do codigo
        uses: actions/checkout@v4
      - name: Rodando Test Job 2
        run: echo "Another test"
